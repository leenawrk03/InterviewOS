export const environment = {
  /**
   * Empty: in production Spring Boot serves the Angular bundle, so the API
   * lives on the same origin and every call is a relative path.
   */
  apiBaseUrl: '',
};
